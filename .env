# Get-Process | Where-Object {$_.ProcessName -like "*race_vision*"} | Stop-Process -Force

# iRacing exposes telemetry through a Windows named memory-mapped file. 
# My app maps that file into its address space and reads the triple-buffered 
# data structures directly from shared memory.